<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seopftware.fundmytravel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- sms 문자 인증을 위한 permission -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- 내 폰 번호를 가져오기 위한 permission -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- some devices need it -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera2.full" />
    <uses-feature android:name="android.hardware.camera2.autofocus" />

    <application
        android:name=".util.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.Intro_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.Login_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".util.streaming.Streaming_Acticity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activity.Home_Activity" />
        <activity android:name=".util.streaming.BeforeStreaming_Activity" />
        <activity android:name=".util.streaming.PlayerStreaming_Activity" />
        <activity
            android:name=".util.chatting.Chatroom_Activity"
            android:label="@string/title_activity_chat_" />
        <activity android:name=".activity.Login_Phone_Activity" />
        <activity android:name=".activity.Login_Phone2_Activity"/>


        <service
            android:name=".util.chatting.Chat_Service"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest> <!-- android:theme="@style/AppTheme.NoActionBar" -->
